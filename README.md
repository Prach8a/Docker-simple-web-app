#Docker Simple Web App

A simple web application containerized using Docker.
This project demonstrates how to package a web application into a Docker container and run it consistently across environments.

###Features

Simple web application

Dockerized for easy deployment

Lightweight and beginner-friendly

Demonstrates containerized application workflow
